meta:
  environment: nodejs
  toolchain:
    name: nodejs
    version: 18
build:
  - cd frontend && npm install && npm run build
  - cd backend && npm install
run:
  command: cd backend && node app.js
  containerPort: 5000